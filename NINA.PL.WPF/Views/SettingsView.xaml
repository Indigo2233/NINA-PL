<UserControl x:Class="NINA.PL.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <StackPanel Margin="16">
        <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>
        <GroupBox Header="User profile (JSON)">
            <Grid Margin="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox Text="{Binding ProfilePath, UpdateSourceTrigger=PropertyChanged}" MinHeight="36" Margin="0,0,0,8"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Content="Browse…" Command="{Binding BrowseProfileCommand}" Margin="0,0,8,0"/>
                    <Button Content="Load" Command="{Binding LoadProfileCommand}" Margin="0,0,8,0"/>
                    <Button Content="Save" Command="{Binding SaveProfileCommand}" Margin="0,0,8,0"/>
                    <Button Content="Create / load default" Command="{Binding EnsureDefaultProfileCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
        <TextBlock Margin="0,16,0,0" TextWrapping="Wrap" Foreground="{StaticResource Brush.TextSecondary}"
                   Text="Profiles are stored under %AppData%\NINA-PL\profiles. Use Load/Save to sync the in-memory UserProfile with disk."/>

        <GroupBox Header="Observer location" Margin="0,16,0,0">
            <Grid Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Latitude (°)" VerticalAlignment="Center" Foreground="{StaticResource Brush.TextSecondary}"/>
                <TextBox Grid.Column="1" Text="{Binding ObserverLatitude, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="1" Text="Longitude (°)" VerticalAlignment="Center" Foreground="{StaticResource Brush.TextSecondary}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ObserverLongitude, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </GroupBox>
        <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource Brush.TextSecondary}" FontSize="11"
                   Text="Observer location is used by astronomical conditions (Sun/Moon altitude, Twilight) in the Sequencer."/>
    </StackPanel>
</UserControl>
